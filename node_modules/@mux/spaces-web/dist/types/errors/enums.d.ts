export interface SignalingError {
    type: number;
    message: string;
    visibility: number;
}
export declare enum SignalingErrorType {
    Unknown = 0,
    Disconnected = 1,
    HitTimeLimit = 2,
    DuplicateParticipantRejected = 3,
    PeerConnectionClosed = 4,
    InvalidPendingTrack = 5,
    InvalidJWT = 6,
    ParticipantLimitReached = 7
}
